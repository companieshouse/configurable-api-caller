sonar.projectName=configurable-api-caller
sonar.projectKey=uk.gov.companieshouse:configurable-api-caller

sonar.host.url=${env.CODE_ANALYSIS_HOST_URL}
sonar.token=${env.CODE_ANALYSIS_TOKEN}

sonar.sources=src
